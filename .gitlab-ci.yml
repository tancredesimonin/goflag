# Layer 1 — vulnerability scanning (infrastructure/gitlab-templates)
include:
  - project: "tancredesimonin-indie/infrastructure"
    ref: main
    file: "gitlab-templates/security-trivy-deps.yml"
  - project: "tancredesimonin-indie/infrastructure"
    ref: main
    file: "gitlab-templates/security-pnpm-audit.yml"
  - project: "tancredesimonin-indie/infrastructure"
    ref: main
    file: "gitlab-templates/security-scheduled.yml"

default:
  image: node:22.23.1
  cache:
    key:
      files:
        - pnpm-lock.yaml
    paths:
      - .pnpm-store
      - node_modules

stages:
  - install
  - verify
  - test
  - security
  - release

variables:
  PNPM_VERSION: "9.15.0"
  CI: "true"

# Jobs that only make sense when the CLI package (or the workspace wiring it
# depends on) actually changed. Docs-only commits are frequent and the test
# suite boots HTTP servers and Chromium — several minutes for a README edit,
# against a 400-minute monthly budget.
#
# Deliberately NOT applied to lint / typecheck / format: those are seconds, and
# a merge request whose every job is filtered out produces an empty pipeline,
# which GitLab treats as a failure.
.cli_changes: &cli_changes
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        - packages/cli/**/*
        - package.json
        - pnpm-lock.yaml
        - pnpm-workspace.yaml
        - .gitlab-ci.yml
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

.pnpm: &pnpm
  before_script:
    - corepack enable
    - corepack prepare pnpm@${PNPM_VERSION} --activate
    - pnpm config set store-dir .pnpm-store
    - pnpm install --frozen-lockfile

install:
  stage: install
  <<: *pnpm
  script:
    - pnpm --version
    - node --version
  artifacts:
    paths:
      - node_modules
    expire_in: 1 hour

lint:
  stage: verify
  <<: *pnpm
  script:
    - pnpm lint

typecheck:
  stage: verify
  <<: *pnpm
  script:
    - pnpm typecheck

format-check:
  stage: verify
  <<: *pnpm
  script:
    - pnpm format:check

build:
  stage: verify
  <<: *pnpm
  script:
    - pnpm build

test:unit:
  stage: test
  <<: [*pnpm, *cli_changes]
  script:
    - pnpm test:unit

test:integration:
  stage: test
  # Playwright image so Chromium is preinstalled and the headless
  # integration tests can run without a separate `playwright install`.
  image: mcr.microsoft.com/playwright:v1.59.1-noble
  <<: [*pnpm, *cli_changes]
  script:
    - pnpm test:integration

# The tarball is what users get; the repository is not. Everything below this
# line exists because a monorepo hides packaging bugs: `dist/` is present
# because we just built it, dependencies resolve because the workspace is
# there. Neither is true in someone else's `node_modules`.
#
# So: pack, install the tarball into an empty directory with npm — not pnpm,
# not the workspace — and run the binary the way `npx goflag` would.
test:package:
  stage: test
  <<: [*pnpm, *cli_changes]
  script:
    - pnpm --filter goflag build
    # `pnpm pack` prints the absolute tarball path as its last line.
    - TARBALL="$(cd packages/cli && pnpm pack --pack-destination "$CI_PROJECT_DIR" | tail -1)"
    - echo "packed $TARBALL"

    # The npm page and the licence come from files staged by `prepack`. If the
    # hook regresses, the package still installs and the page is silently blank.
    #
    # Listed to a file rather than piped: `grep -q` exits at the first match and
    # closes the pipe, `tar` takes SIGPIPE, and the runner's `pipefail` turns a
    # passing assertion into exit 141.
    - tar -tzf "$TARBALL" > tarball-contents.txt
    - grep -qx 'package/README.md' tarball-contents.txt
    - grep -qx 'package/LICENSE' tarball-contents.txt

    - mkdir -p /tmp/goflag-install && cd /tmp/goflag-install
    - npm init -y > /dev/null
    - npm install "$TARBALL"

    # `playwright` is an *optional peer*: headless mode asks for it at runtime
    # and degrades with a clear message. As an optionalDependency it installed
    # by default, and its postinstall downloads Chromium — a ~150 MB surprise
    # for someone who only wants `--static`. This asserts it stays absent.
    - test ! -d node_modules/playwright

    - ./node_modules/.bin/goflag --version
    - ./node_modules/.bin/goflag --help > /dev/null

# Publishing happens on a tag and nowhere else: a version is a promise, and
# `develop` makes no promises. Tags come from `pnpm release`
# (commit-and-tag-version), which bumps package.json and tags in one step.
publish:npm:
  stage: release
  <<: *pnpm
  rules:
    - if: $CI_COMMIT_TAG
  script:
    # commit-and-tag-version writes both, so a mismatch means the tag was made
    # by hand. Publishing it would put a version on npm that matches no commit.
    - PKG_VERSION="$(node -p "require('./packages/cli/package.json').version")"
    - |
      if [ "$CI_COMMIT_TAG" != "v$PKG_VERSION" ]; then
        echo "tag $CI_COMMIT_TAG does not match package version $PKG_VERSION" >&2
        exit 1
      fi
    - npm config set "//registry.npmjs.org/:_authToken=${NPM_TOKEN}"
    - pnpm --filter goflag publish --access public --no-git-checks

# Merging develop into main is the decision to publish. This job turns that
# merge into a version and a tag; `publish:npm` above turns the tag into a
# package. Two jobs rather than one because they fail for different reasons:
# a bad version is a repository problem, a failed publish is a registry one.
#
# Needs RELEASE_TOKEN — a project access token with the Maintainer role and
# `write_repository`. The job token cannot push, and a push made with it would
# not trigger the tag pipeline that publishes.
release:
  stage: release
  <<: *pnpm
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
  variables:
    # commit-and-tag-version reads the whole history and every tag to work out
    # the bump. The default shallow clone would make it invent 1.0.0 forever.
    GIT_DEPTH: 0
  script:
    - git config user.name "goflag release"
    - git config user.email "39413504+tancredesimonin@users.noreply.github.com"
    - git remote set-url origin "https://oauth2:${RELEASE_TOKEN}@gitlab.com/${CI_PROJECT_PATH}.git"
    - git fetch origin --tags --quiet

    # CI checks out a detached HEAD; commit-and-tag-version commits, so it needs
    # a branch to commit onto.
    - git checkout -B main "$CI_COMMIT_SHA"

    # A merge to main is not always a release. Docs, chores and refactors are
    # merged too, and publishing on those would spend a version number on
    # nothing — then open a merge request on every consuming site to pin it.
    - LAST_TAG="$(git describe --tags --abbrev=0 2>/dev/null || true)"
    - RANGE="${LAST_TAG:+$LAST_TAG..HEAD}"
    - |
      if ! git log --format=%B $RANGE | grep -qE '^(feat|fix|perf)(\(.+\))?!?:|^[a-z]+(\(.+\))?!:|^BREAKING[ -]CHANGE'; then
        echo "No feat/fix/perf/breaking commit since ${LAST_TAG:-the first commit} — nothing to release."
        exit 0
      fi

    - pnpm --filter goflag release
    - VERSION="$(node -p "require('./packages/cli/package.json').version")"
    - echo "releasing v$VERSION"

    # Tag last-ish: pushing the branch and the tag together means the tag
    # pipeline (which publishes) only ever sees a commit that is already on main.
    - git push origin HEAD:main --follow-tags

    # The release commit carries the new version and the changelog. If develop
    # does not receive it, the next merge to main sees two different versions of
    # package.json and resolves the conflict in whichever direction it likes.
    #
    # Loud on failure: the package is already published at this point, so a
    # silent skip would leave develop quietly wrong for weeks.
    - |
      if ! git push origin HEAD:develop; then
        echo "v$VERSION is published, but develop did not fast-forward." >&2
        echo "Someone pushed to develop during the release. Merge main into develop by hand." >&2
        exit 1
      fi

# Trivy scans the working dir; disable the cache so only repo sources
# (incl. pnpm-lock.yaml) are scanned, not cached third-party packages.
trivy-deps:
  cache: {}

trivy-scheduled:
  cache: {}

workflow:
  rules:
    # First, and deliberately so. A tag pipeline shares the commit message of
    # the release commit below, so any rule matching that message would take
    # the publishing pipeline down with it. This is the same trap as putting
    # `[skip ci]` in the release commit: it skips the tag too.
    - if: $CI_COMMIT_TAG

    # The `release` job pushes its own commit back to main, which triggers a
    # pipeline, which would run `release` again. Cut the loop here rather than
    # in the job, so the whole pipeline is skipped instead of merely idle.
    - if: $CI_COMMIT_BRANCH == "main" && $CI_COMMIT_MESSAGE =~ /^chore\(release\)/
      when: never

    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == "develop"
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_PIPELINE_SOURCE == "schedule"
